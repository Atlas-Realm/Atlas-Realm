{
  "name": "server",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "db:push": "drizzle-kit push",
    "db:seed": "bun run src/db/seed.ts",
    "db:setup": "drizzle-kit migrate",
    "db:setup:seed": "drizzle-kit migrate && bun run src/db/seed.ts",
    "bootstrap:prod": "drizzle-kit migrate"
  },
  "dependencies": {
    "@hono/zod-openapi": "^0.19.6",
    "@scalar/hono-api-reference": "^0.9.46",
    "drizzle-orm": "^0.44.0",
    "hono": "^4.12.3",
    "ioredis": "^5.6.1",
    "postgres": "^3.4.7",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.0"
  }
}
